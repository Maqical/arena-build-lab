export default function Loading() {
  return <main className="route-loading" role="status" aria-live="polite">
    <span aria-hidden="true" />
    <strong>Loading view</strong>
    <p>Reading the local Arena warehouse...</p>
  </main>;
}
