import { ProPlayersBrowser } from "@/components/ProPlayersBrowser";import { RegionToggle } from "@/components/RegionToggle";import { proPlayers } from "@/lib/pro-players";import { selectedRegion } from "@/lib/region";
export const dynamic="force-dynamic";
export default async function ProsPage({searchParams}:{searchParams:Promise<{region?:string}>}){const query=await searchParams,region=selectedRegion(query.region);return <main className="insight-page"><header className="collection-header"><div><span className="eyebrow">Public tracked cohort</span><h1>Pro & High-Elo Watch</h1><p>Follow public players, inspect locally ingested Arena matches, and copy exact recorded setups.</p></div><RegionToggle region={region} pathname="/pros"/></header><ProPlayersBrowser initial={proPlayers(region)}/></main>;}
